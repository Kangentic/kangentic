## What's New
- File autocomplete in description editors - type `@` to search and insert file paths from your project

## Bug Fixes
- Fixed terminal output getting truncated due to a resize/scrollback race condition
- Fixed stale command labels persisting on task cards after moving a user-paused task
- Header shortcut pills that overflow the available space are now hidden instead of clipped
- Manually paused tasks no longer auto-resume when moved between columns
- Fixed command palette dropdown not appearing in the Command Terminal overlay
