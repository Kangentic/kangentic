<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <!--
    Required to bind the in-process MCP HTTP server's loopback listener
    (src/main/agent/mcp-http-server.ts). Hardened runtime denies inbound
    network sockets without this entitlement, even on 127.0.0.1, so
    signed/notarized builds would fail to start the MCP server. Without
    it, every kangentic_* MCP tool call from a Claude Code session
    spawned by Kangentic would error out at request time.
  -->
  <key>com.apple.security.network.server</key>
  <true/>
  <!-- Microphone access for voice-to-text dictation (getUserMedia in the
       renderer). Paired with NSMicrophoneUsageDescription in
       electron-builder.yml mac.extendInfo. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
