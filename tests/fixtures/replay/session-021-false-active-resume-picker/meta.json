{
  "sessionId": "00000000-0000-0000-0000-000000000000",
  "capturedAt": "2026-07-05",
  "note": "Real capture of task #331 false-ACTIVE-pinned-after-resume-picker (Kangentic session de06e459, sanitized). The session was spawned with --resume; the CLI ran its resume-picker context-reload turn, a CLI-INTERNAL turn that fires NO turn hooks (events.jsonl carries only session_start source=resume, then nothing until the real user prompt ~10 minutes later). While the reload summary generated, status.json output tokens grew and the heartbeat correctly force-thinked (idle -> thinking, one force-thinking transition). Then the reload finished and Claude parked at its prompt with NO Stop/idle_hint to clear turnActive. Pre-fix the parked-TUI statusline kept rewriting status.json every ~10s and each write passed the keep-warm gate (thinking && !idleHintPending -> markThinkingSignal), refreshing lastSignalAt forever, so the 180s stale-thinking watchdog was STARVED and the card pinned ACTIVE until the next real prompt. The fix gates keep-warm on output-token GROWTH (proof of work); frozen-output churn stops re-warming, and once the parked-statusline PTY chunks go quiet (a 252.7s gap at 15:55:46 -> 16:00:00) the signal-or-pty-output anchor freezes and stale-thinking self-heals to idle. status-deltas.jsonl is the captured stream (output frozen at 1144 across the whole parked window) with ONE synthetic baseline delta prepended ({ts 1783266611000, output 0}): the trace recorder does not capture the transcript-fallback usage seed (#286) that gave the live engine a previousUsage for the very first status write, so without a baseline the first captured delta (output 1144) has no previous and the reproduced force-think cannot fire. events.jsonl and pty-chunks.jsonl are verbatim captures. Replay via tests/unit/activity-engine-trace-replay.test.ts:replayBundle."
}
