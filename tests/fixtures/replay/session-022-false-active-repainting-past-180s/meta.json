{
  "sessionId": "00000000-0000-0000-0000-000000000000",
  "capturedAt": "2026-07-08",
  "note": "Reconstructed for task #364 (the residual leg #331 left open), grounded in live measurements from the captured incident on session d1d25784 / task #290: a --resume 'full session' launch ran the resume-picker context-reload (session_start source=resume, no turn hooks), the heartbeat force-thought once on output growth (real incident: forceThinking at ts 1783515975712, output frozen at 1408, model claude-sonnet-5, ~573k cached input tokens), then parked with no idle_hint. In the real incident the parked-TUI PTY chunks eventually went quiet and the stale-thinking net self-healed via the OLD signal-or-pty-output anchor's PTY-quiet-gap path (self-heal fired at lastPtyOutputAt+180000ms, ~4.5 min after output actually froze) - task #331 fixed only the lastSignalAt re-warm leg. This bundle is SYNTHESIZED (not a raw capture) to exercise the leg #331 left open: it models a chattier parked TUI whose statusline repaints never leave a >180s gap (pty-chunks.jsonl repaints every 10s continuously for 300s), so under the pre-#364 anchor the signal-or-pty-output base never freezes and the card would stay ACTIVE indefinitely. events.jsonl carries only the session_start (no turn hooks, no trailing real turn), so the ONLY way to heal is the stale-thinking watchdog. Post-#364 the anchor narrows to 'signal' (lastSignalAt only) once turnForcedByHeartbeat is set by the heartbeat's forceThinking(sessionId, true), so the net fires ~180s after output froze (at the frozen lastSignalAt) regardless of the continuing PTY repaints. Replay via tests/unit/activity-engine-trace-replay.test.ts:replayBundle."
}
